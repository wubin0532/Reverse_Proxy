# andey-proxy 安全审查报告

审查日期：2026-09-07。对象：当前仓库的 Go 服务、Vue 管理后台、OpenWrt／LuCI 集成、构建脚本和本地发布包。

源码基线：`0d733252e67c72a1e47a8a36f32d272b7d74eb1a`，审查开始时工作区干净。应用标称版本：`0.2.2`。

## 1．结论

**当前版本存在需要修复的安全边界缺口，不宜直接认定为“可安全暴露公网”。** 本轮列出 **2 项条件性高风险、6 项中风险、1 项低风险**。其中发布包签名异常同时属于发布阻断项。

最重要的问题是：管理会话可能经同主机的代理站点流向后端；LuCI 的只读授权包含可改变配置的程序执行权限；管理入口存在并发限速和慢请求资源保护缺口。另一个独立问题是：本地 `release` 目录全部 5 个 `.run` 包均无法通过当前内置公钥验签。

未发现已确认的、无需任何前提即可远程执行任意系统命令的漏洞。这个结论不能替代实机验证，也不表示不存在其他漏洞。高风险项分别依赖同主机部署／后端不可信、LuCI 限权账号被授予插件只读权限等条件。

本轮没有修改业务源码、配置、防火墙或发布包，也没有部署、执行安装脚本或连接真实 DNS 服务商。仅新增本报告和证据目录；针对性测试在隔离源码副本中运行，使用虚构凭据和本机测试服务器。

## 2．风险清单

| 编号 | 级别 | 问题 | 验证状态 |
|---|---|---|---|
| S01 | 高，条件性 | 管理 Cookie 跨端口发送，并被反向代理转发给后端 | Cookie 选择、后端转发、异源 IP 重放均已本机验证 |
| S02 | 高，条件性 | LuCI 只读 ACL 允许任意参数执行主程序及读取初始密码 | 插件源码＋上游 rpcd 源码确认；未做固件实机复现 |
| S03 | 中 | 登录失败限速缺少原子的请求准入，并发可超过限额 | 同 IP 的 12 次并发均完成密码校验 |
| S04 | 中 | 管理 API 请求体无读取期限，可被未登录慢请求长期占用 | 本机连接验证读取超出写入／空闲超时 |
| S05 | 中，条件性 | 畸形登录请求可绕过限速状态表容量控制 | 构造 5,000 个源地址，保留桶数超过 4,096 |
| S06 | 中 | “首次强制改密”仅限制前端导航，服务端仍签发完整会话 | 强制改密状态下，受保护写路由返回 200 |
| S07 | 中，需已登录 | 凭据测试允许把已保存 DNS Token 发给另一个端点 | 本机接收端收到虚构的已存 Cloudflare Token |
| S08 | 低，本地健壮性 | 加密配置 nonce 长度异常导致启动 panic | 隔离单测已复现 |
| S09 | 中，发布阻断 | 本地 5 个发布包均未通过内置公钥验签 | Go 实际验包路径和 Node.js 独立验证结果一致 |

### S01．管理 Cookie 泄露到代理后端

代码依据：`internal/api/router.go:238` 附近的 `finishLogin` 设置 `andey-proxy_token`，其 `Path=/`；`internal/webproxy/reverse.go:143` 的 `Rewrite` 重建代理头，但没有删除管理 Cookie；`internal/api/router.go:135` 的鉴权仅检查有效 Token。

浏览器 Cookie 不按端口隔离。例如管理员登录 `https://router.local:16606`，随后打开 `https://router.local:8443`，符合 Cookie 发送条件时，后者也会收到管理 Cookie。`HttpOnly` 防止脚本读取，`Secure` 限制传输协议，都不能阻止同主机其他 HTTPS 端口接收 Cookie。依据：[RFC 6265 的 Cookie 隔离说明](https://www.rfc-editor.org/rfc/rfc6265#section-8.5)。

该端口若由插件反向代理，Cookie 会继续发送给后端。后端可能是不同管理者维护的应用、已遭入侵的服务，甚至通过明文 HTTP 连接。接收方拿到 Token 后可直接调用管理 API，不需要重新通过 TOTP。会话到期和敏感操作的二次密码确认仍然有效，但大量站点、转发及凭据配置接口仅要求有效会话。

复现由三个相互补充的检查组成：Go CookieJar 为另一 HTTPS 端口选出登录 Cookie；真实本机后端收到管理 Cookie；该会话从另一模拟 IP 请求 `/api/me` 得到 200。未启动真实浏览器，该跨端口行为同时由规范支持。

修复建议：管理后台使用专用主机名并限制可接受 Host，避免与业务入口共用同一主机身份；代理出站精确移除管理 Cookie，保留业务 Cookie；阻止后端响应设置管理专用 Cookie。使用 `__Host-` 前缀可额外限制 Domain／Path，但**不能解决跨端口隔离**。修复后撤销存量会话，并新增包含 HTTPS 前端与 HTTP 后端的集成测试。

### S02．LuCI 的只读授权包含写入能力

代码依据：`package/openwrt/luci/usr/share/rpcd/acl.d/luci-app-andeyproxy.json:4`—`14`：在 `read` 中授权 `file.exec`，并对 `/usr/bin/andey-proxy` 授予 `exec`；同时允许读取 `/etc/andey-proxy/initial-password`。

页面仅需运行 `andey-proxy -v`，ACL 却授权了整个可执行文件。上游 rpcd 在可执行文件路径已获授权时，不再要求完整参数字符串另行获批。因此，插件只读用户可提交其他参数。主程序的 `-reset-totp` 会修改安全配置；`-cd` 会创建配置文件及收紧目录权限。依据：[OpenWrt rpcd 的执行权限实现](https://github.com/openwrt/rpcd/blob/master/file.c) 中 `rpc_file_exec_run` 的权限判断。

限制：重置现有实例的 TOTP 要求服务停止、配置锁可取得，不能在正常持锁运行时直接完成。尚未检查实际固件的 rpcd 版本及是否存在限权用户。如果系统只有全权 root 管理员，此项不会新增 root 权限，但 ACL 的只读语义仍不成立。初始密码读取还会在首次改密之前使只读授权持有人获得插件管理员凭据。

修复建议：将版本查询改为仅返回版本号的专用 RPC，或在确认固件支持后，仅授权完整固定命令 `/usr/bin/andey-proxy -v`；初始密码读取应属于明确的管理员权限。实机增加“只读账号不得重置 TOTP、不得启动其他参数实例、不得读取初始密码”的否定测试。

### S03．并发登录绕过次数限制

代码依据：`internal/api/router.go:156`—`198`；`internal/api/ratelimit.go:20`—`57`。

处理顺序为“检查已发生失败 → 读取请求体 → bcrypt → 记录失败”。不同请求之间没有原子的名额预占。在任何一条请求记入失败之前，多个请求都能通过检查。

本机同步释放 12 个同 IP 请求，结果为 12 个 403、0 个 429，全部进入密码校验；预期限额为 5 次。可进一步利用慢请求先通过检查，再集中提交猜测。它不会无限解除后续限速，但能使每批尝试数取决于并发资源，而非配置限额，并增加 CPU 消耗。共用同一限速器模式的二次密码确认入口也应一并检查。

修复建议：在读取／验证前原子预占尝试名额；设置全局密码计算并发预算及每 IP 的在途上限。验证失败次数和请求频率可分别限制，避免成功登录成为无限计算资源入口。

### S04．未认证请求体可长期阻塞

代码依据：`main.go:187`—`190` 的管理服务器没有 `ReadTimeout`；`internal/api/response.go:47` 的 `DecodeBody` 仅设置字节上限；登录端点无需认证。

攻击者发送完整请求头，声明请求体长度，随后不完成请求体。`ReadHeaderTimeout` 已经过了保护阶段；`IdleTimeout` 针对请求之间的空闲；`WriteTimeout` 不会自动终止阻塞中的请求体读取。大量这类连接可占用文件描述符和 goroutine，路由器尤其敏感。

本机复现使用同等超时结构，将写入／空闲超时缩短至 50 ms，200 ms 后读取仍被阻塞，关闭客户端连接后才退出。此测试验证机制，不代表做过路由器耗尽资源压测。站点 HTTP 服务同样缺少完整连接预算，其长连接需求应分开设计。

修复建议：普通 JSON API 设置短而明确的请求体读取期限，上传更新包采用单独可配置的期限／最低传输速率；增加总连接数、每来源连接数和在途请求预算。不要因为允许 100 MiB 上传而让登录端点也无限等待。

### S05．畸形 JSON 导致限速桶绕过容量约束

代码依据：`internal/api/ratelimit.go:33` 在查询时直接写入新桶；容量淘汰仅在 `record` 中；`internal/api/router.go:166` 对 JSON 错误直接返回 400，没有调用 `record`。

因此，不同真实直连来源发送畸形 JSON 可留下空桶，跳过 4,096 桶上限和全局清理。本机以 5,000 个不同 `RemoteAddr` 调用真实路由，状态表保留了 5,000 桶。

利用前提：攻击者能使用大量实际可完成连接的源 IP，例如可用的 IPv6 地址段或分布式节点。代码不信任 X-Forwarded-For，因此不能仅修改请求头实现；测试中的来源地址为模拟值，不代表实测网络可达性。

修复建议：纯查询不得插入空桶；所有新桶插入统一执行容量控制；周期淘汰过期和空桶，并针对畸形请求设置轻量限速。

### S06．首次强制改密没有服务端权限约束

代码依据：`main.go:111` 设置 `MustChangePassword`；`web/src/router/index.js:51` 附近只做导航限制；`internal/api/router.go:135` 不检查该标记，登录仍签发完整会话。

复现将该标记设为 true，使用初始密码状态登录后访问同一认证分组内的模拟写操作，返回 200。现有真实配置变更路由均挂载在该分组，未见独立首次改密检查。

首次密码是高熵随机值，不能把本项解释成弱默认口令。但它会写入 `initial-password` 文件以及 stderr；OpenWrt procd 又将 stderr 交给系统日志。未改密时，任何提前获得该凭据的人可绕过页面要求直接调用管理接口，“一次性”目前只是提示语义。

修复建议：首次登录签发仅用于改密的受限会话，或者在服务端仅放行个人状态、改密、退出等明确白名单；初始凭据增加有效期，改密后清理初始文件并撤销会话。避免把初始口令写入可能被统一采集的系统日志。

### S07．测试端点可外送已保存的 DNS Token

代码依据：`internal/ddns/api.go:511`—`577`；`internal/ddns/cloudflare.go:71` 附近。

`/api/providers/test` 接受服务商 ID 和调用方提供的 Endpoint。当未提供 Key／Secret 时，接口从现有配置补入秘密，但允许保留调用方指定的新 Endpoint。随后 Cloudflare 实现向该端点发送 Bearer Token。

本机复现：只提交虚构服务商 ID、域名和本机 Endpoint，没有提供已存 Token 或管理密码；接收端收到 `Authorization: Bearer SYNTHETIC-CLOUDFLARE-TOKEN`。远程非回环端点需使用 HTTPS，但攻击者控制的合法 HTTPS 服务同样可接收。生产凭据未被读取或发送。

本项需要已登录管理会话，不是公开 SSRF。风险在于突破“列表不返回秘密、备份需二次密码”的凭据读取边界；与 S01 的会话泄露结合后，影响可扩大到 DNS 控制权。现有单管理员模型本身权限很大，因此按中风险、已认证凭据外送问题列出。

修复建议：复用已存秘密时，将服务商类型和端点绑定到原配置；更改端点必须二次认证并明确提示会向新地址发送凭据，或要求重新输入凭据。更新服务商接口也要执行相同规则，不能只修复测试接口。

### S08．损坏的本地配置 nonce 触发 panic

代码依据：`internal/config/config.go:438`—`466`。

配置读取时只检查 nonce 的 Base64 编码是否有效，没有验证解码长度就调用 GCM.Open；长度错误引发 panic。本机用 1 字节 nonce 复现 `crypto/cipher: incorrect nonce length given to GCM`。

前提是本地配置文件损坏或被有写权限者修改。没有发现未登录网络写入该文件的路径；备份导入使用另一个已校验 nonce 长度的实现。因此本项是低风险启动健壮性缺陷，不应误报为远程拒绝服务。

修复建议：调用 GCM.Open 前检查 nonce 长度和密文最小长度，返回明确错误并提供安全恢复流程。

### S09．本地发布包不能由当前内置公钥验证

代码依据：`internal/upgrade/manager.go:33` 和 `437` 附近；构建签名逻辑在 `scripts/build-release.sh:265`—`275`。产物位于本地 `release` 目录。

对 x86_64、arm64、armv7、mips、mipsle 五个 `.run` 包分别执行仓库的 `TestBuiltSignedPackage`，均报告“更新包签名验证失败”。再用 Node.js `crypto.verify` 独立验证原始 manifest 与签名，五个结果均为 false。

同时确认：

- `checksums.txt` 对 10 个 `.run`／`.ipk` 文件的摘要核对全部通过。
- 5 个包内二进制均与各自 manifest 的 SHA-256 匹配。
- 相应架构的 `.ipk` 与 `.run` 中主程序字节一致。
- 抽查 ARM64 的构建信息：Go 1.27.1，提交 `add8e188d29cf3e0a64d45e8347a82b30ae7f20e`，`vcs.modified=true`；当前源码提交不同。不能将当前源码审查结果直接等同于这批二进制。

**摘要一致只能说明本地文件之间自洽，不能代替来源认证。** 当前证据不足以断言文件被恶意篡改，也未确定是否使用了错误私钥。当前源码实现会拒绝这些包，属于安全机制有效阻断；不得为了通过更新而删除验签。

另需区分：Web 更新路径仅提取并验证二进制，不执行脚本头；直接 `sh xxx.run` 的安装路径先执行脚本并解压安装，没有独立验签。这两条路径不能宣称具有相同签名保护。脚本头、初始化脚本、卸载脚本也不在当前仅包含二进制摘要的 manifest 保护范围内。

修复建议：在可信环境核对发布公私钥配对，使用干净提交重新构建；固定工具链；为最终分发文件提供可信渠道的独立签名／证明。签名失败作为发布硬门禁，拒绝继续上传或人工绕过。无需把私钥写入报告或代码仓库。

## 3．已确认存在的安全措施

| 领域 | 当前保护 | 边界 |
|---|---|---|
| 登录与会话 | 随机初始密码、bcrypt、256 位随机 Token、服务端摘要存储、Secure／HttpOnly／SameSite Cookie、会话数量与绝对有效期上限 | S01、S03、S06 仍需处理 |
| TOTP | 挑战绑定来源 IP、次数和期限限制、恢复码哈希、会话撤销、计数器持久化 | 写盘失败仅记日志仍返回成功；建议改为失败关闭，避免重启后重放窗口 |
| API 访问控制 | 模块路由统一进入 requireAuth；Origin／Sec-Fetch-Site 校验 | 已登录会话拥有广泛管理能力；首次改密限制未落在服务端 |
| 管理页面 | CSP、nosniff、禁止嵌入、HTTPS 下 HSTS；前端源文件中未见主动 v-html／innerHTML 渲染入口 | 没有做浏览器全流程 XSS 动态模糊测试 |
| 文件服务 | os.OpenRoot 约束符号链接逃逸；歧义路径拒绝，既有回归测试通过 | 可配置任意 RootDir，未禁止敏感目录、隐藏文件或目录列表；不要发布含配置／密钥的目录 |
| 反向代理 | 清理并重建转发来源头；后端 TLS 跳过验证需显式开启；安全方法才自动重试 | BasicAuth 默认仍向后端透传，应视后端信任边界决定是否移除；UA 名单不构成身份认证 |
| 端口转发 | TCP／UDP 各 256 的共享连接／会话预算、直连 IP 名单 | UDP 转发不等同于用户认证，也不能消除外部 UDP 服务的反射风险 |
| 配置与备份 | AES-256-GCM、权限收紧、原子写、文件锁；备份 scrypt 固定参数及并发限制 | 加密密钥在配置目录相邻位置；整机／root 失陷后无法保密 |
| DNS 请求 | 官方默认 HTTPS、禁止提供商重定向、返回体大小与请求时限约束、部分错误脱敏 | S07 的“秘密和端点绑定”缺失 |
| 系统命令 | 使用 exec.Command 参数数组，检查范围内未见用户输入拼接 shell 执行 | 进程默认 root，文件服务、配置恢复、LuCI 参数入口仍需最小权限设计 |
| 升级解析 | Ed25519、摘要、大小、ELF 平台检查；不执行上传脚本 | 当前实际发布包验签失败；普通测试会跳过实际产物验签 |

补充加固项，不与以上漏洞重复计数：

1. 管理端默认监听全部网卡；CLI 有 `-listen`，OpenWrt UCI／LuCI 尚未暴露该设置。未核查实机 WAN 防火墙及 IPv6，因此不能推断当前设备一定对公网开放。
2. 服务默认拥有 root 权限；可将常规代理运行与防火墙／更新操作拆分。自定义配置目录校验应统一规范化、处理符号链接并限制为专用目录，防止误将系统目录 chmod 为 0700。
3. `Config.Restore` 仅检查账号字段非空，没有复用每个模块的完整校验；备份需当前密码，因此主要是高权限误操作和恢复一致性风险。
4. Webhook URL 可能在路径或查询参数携带密钥，不应因禁止 userinfo 就视为“不含凭据”。接口明文回显完整地址，通用正则日志脱敏无法可靠覆盖路径 Token；建议结构化脱敏并默认 HTTPS。
5. `.run`／IPK 卸载脚本删除配置目录时没有同步删除相邻的 `/etc/andey-proxy.key`；需明确保留策略和彻底卸载行为。残留密钥与另处配置备份组合仍可解密旧配置。
6. CI 的第三方 Action 使用版本标签，扫描工具使用 `@latest`，未设置显式最小 `permissions`；建议固定可审计版本／提交并明确发布权限。未检查远端仓库的分支保护和 Secret 配置。

## 4．测试与依赖结果

| 验证 | 本轮结果 |
|---|---|
| 原始工作区，Go 1.27.1，`go test -race -count=1 ./...` | 通过；发现命令还遍历到 node_modules 中的 Go 包 |
| 干净源码隔离副本，Go 1.26.6，`go test -race -count=1 ./...` | 通过，包含新增的审计复现用例 |
| Go 1.26.6，`go vet ./...` | 通过 |
| 前端 `npm test` | 2 个文件、4 项测试通过 |
| `npm audit --package-lock-only --json` | 341 个依赖统计，已报告漏洞总数 0 |
| govulncheck v1.7.0，Go 1.26.6 | 当前调用的漏洞 0、已导入包漏洞 0；模块层面 3 项提示 |
| 针对性安全复现 | 8 项均成功复现预期的不安全行为；PASS 表示复现成功，不表示漏洞修复 |
| 当前实际 `.run` 包验签 | 5／5 失败；独立加密库复核一致 |

模块层面的提示如下。扫描器未发现当前代码导入或调用其受影响包，不能直接算作本插件已可利用的漏洞：

- [GO-2026-6180](https://pkg.go.dev/vuln/GO-2026-6180)：`golang.org/x/mod@v0.38.0`，修复版本 v0.40.0。
- [GO-2026-6179](https://pkg.go.dev/vuln/GO-2026-6179)：同一模块的另一项校验问题，修复版本 v0.40.0。
- [GO-2026-5932](https://pkg.go.dev/vuln/GO-2026-5932)：`golang.org/x/crypto/openpgp` 的维护与设计风险；当前未发现调用该包。

依赖结果是本轮数据库快照，不保证未来不存在新披露。没有对所有历史二进制执行单独的漏洞和恶意代码分析；`dist` 中还存在旧的 luckyx_dev 产物，应与当前 release 区分管理。已跟踪文件名中未见 `.pem`、`.key` 或 `.env` 文件，但这不是完整 Git 历史密钥扫描或“无秘密”保证。

## 5．处置优先级

**优先处理：** 暂缓分发当前验签失败的 `.run` 包；明确管理专用主机身份并避免会话跨到业务后端；收紧 LuCI 的执行和初始密码读取权限。

**随后修复：** 给密码验证增加原子准入和总并发预算；给普通 API 请求体加期限；修复限速空桶增长；服务端执行首次改密约束；将 DNS 秘密与允许的端点绑定。

**发布前验收：** 对新包逐架构执行真实签名验证；进行实机 LuCI 限权账号验证；核对管理端实际监听地址、IPv4／IPv6 WAN 入站策略和专用域名；确认业务站点无法收到管理 Cookie。保留现有回归测试，并将本轮针对性复现改为阻止不安全行为的回归测试。

本轮未验证真实路由器、生产配置、已安装包版本、上游后端的路径规范化差异、真实浏览器交互、远端 CI 权限及密钥保管，也未进行破坏性负载测试。静态代码、隔离复现、依赖扫描及本地发布检查共同支持本报告，不能扩展为生产环境已经安全的证明。

## 6．证据与复现材料

证据保存在同目录的 `security-audit-2026-09-07` 文件夹：

- `targeted-reproductions.log`：8 项定向复现结果。
- `*-security_audit*.go.txt`：复现测试源码，使用 `.txt` 后缀，避免混入正常构建。
- `go126-race.log`、`go127-race.log`、`go-vet.log`、`frontend-tests.log`：原始测试输出。
- `govulncheck.log`、`npm-audit.json`：依赖扫描结果。
- `release-signature-tests.log`、`independent-signatures.json`：实际发布包的双重验签结果。
- `release-checksums.log`、`release-arm64-buildinfo.log`：摘要及抽样构建来源信息。

复现时应先复制当前基线到隔离目录，将 `.go.txt` 恢复到对应 internal 子包并移除 `.txt` 后缀，再执行 `GOTOOLCHAIN=go1.26.6 go test -v -run '^TestAudit' ./internal/api ./internal/webproxy ./internal/config ./internal/ddns`。测试使用临时配置和虚构秘密，不需要生产环境凭据。
